import { Box } from "@mui/material";
import { styled } from "@mui/system";

// Styling for a flex container with space between items.
const FlexBetween = styled(Box)({
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
});

export default FlexBetween;
